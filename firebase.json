{
    "firestore": [
        {
            "database": "landingbrecomperudb",
            "rules": "firestore.rules",
            "indexes": "firestore.indexes.json"
        }
    ],
    "storage": {
        "rules": "storage.rules"
    }
}